version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd frontend && npm ci
  build:
    commands:
      - cd frontend && VITE_API_URL=https://api.cybrmoon.space npm run build
artifacts:
  base-directory: frontend/dist
  files:
    - '**/*'
